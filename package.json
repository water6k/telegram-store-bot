{
  "name": "telegram-store-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Telegram store bot (Softools-style) — Vercel + Supabase + grammY",
  "scripts": {
    "setup-webhook": "node --env-file=.env scripts/setup-webhook.js",
    "seed": "node --env-file=.env scripts/seed.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "grammy": "^1.30.0"
  }
}
